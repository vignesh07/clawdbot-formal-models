SPECIFICATION Spec
INVARIANT Inv_DenyWins
INVARIANT Inv_AllowedImpliesDeclared
INVARIANT Inv_MustBeAllowlisted

CONSTANTS
  Commands = {system_run, system_notify, camera_snap, location_get}
  PlatformDefaults = {system_run, system_notify}
  ExtraAllow = {camera_snap}
  Deny = {system_run}
  DeclaredByNode = {system_notify, camera_snap}
  Cmd = system_notify
